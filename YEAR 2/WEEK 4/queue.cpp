#include <iostream>

class node {
    public:
    string name;
    node* next;
    
    node(string name){
        name = link;
        next=nullptr;
    }

};

class queue {
    public:
    int size;
    node* front,back;

    queue(){
        front = nullptr;
        back = nullptr;
        size = 0;
    }

    void enqueue(){
    string dequeue()
        if (front == nullptr){
            cout << "queue is empty" << enld;
        } else {
            node* temp = front;
            string name = temp->name;
            front = front -> next;
            seize--;
            return name;
        }

    }

};

int main(){

    return 0;
}